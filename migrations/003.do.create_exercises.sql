CREATE TABLE exercises (
    id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
    assigned_category INTEGER REFERENCES categories(id) ON DELETE CASCADE NOT NULL,
    exercise_name TEXT NOT NULL
);